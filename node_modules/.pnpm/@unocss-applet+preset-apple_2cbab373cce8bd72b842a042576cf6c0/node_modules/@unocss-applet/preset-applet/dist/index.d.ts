import * as _unocss_core from '@unocss/core';
import { SourceCodeTransformer } from '@unocss/core';
import * as _unocss_preset_wind3 from '@unocss/preset-wind3';
import { PresetWind3Options } from '@unocss/preset-wind3';

/**
 * @see https://github.com/unocss/unocss/blob/main/packages-presets/preset-wind3/src/index.ts#L16 for `PresetWind3Options`
 * @see https://github.com/unocss/unocss/blob/main/packages-presets/preset-mini/src/index.ts#L33 for `PresetMiniOptions`
 */
interface PresetAppletOptions extends PresetWind3Options {
    /**
     * Unsupported characters in applet, will be added to the default value
     * @default
     * ```
     * ['.', ':', '%', '!', '#', '(', ')', '[', '/', ']', ',', '$', '{', '}', '@', '+', '^', '&', '<', '>', '\'', '\\', '"', '?', '*']
     * ```
     */
    unsupportedChars?: string[];
    /**
     * Space Between and Divide Width Elements
     * @default
     * ```
     * ['view', 'button', 'text', 'image']
     * ```
     */
    betweenElements?: string[];
    /**
     * Space Between and Divide Width Elements
     * @default
     * ```
     * ['view', 'button', 'text', 'image']
     * ```
     */
    wildcardElements?: string[];
}
interface TransformerAppletOptions extends Pick<PresetAppletOptions, 'unsupportedChars'> {
}

declare function transformerApplet(options?: TransformerAppletOptions): SourceCodeTransformer;

declare const presetApplet: _unocss_core.PresetFactory<_unocss_preset_wind3.Theme, PresetAppletOptions>;

export { presetApplet as default, presetApplet, transformerApplet };
export type { PresetAppletOptions, TransformerAppletOptions };
